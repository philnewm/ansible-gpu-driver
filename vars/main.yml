---

supported_vendors:
  - nvidia
  - amd

amd_driver_packages:
  - hip-runtime-amd
  - rocm-opencl-runtime

nvidia_driver_packages:
  RedHat:
    - akmod-nvidia
    - xorg-x11-drv-nvidia-cuda
  Debian:
    - nvidia-driver-570

debian_release_map:
  "bookworm": "jammy"

...
